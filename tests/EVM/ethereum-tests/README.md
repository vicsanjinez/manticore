This directory contains a port of tests from github.com/ethereum/ethereum-test/.

If you need to regenerate the test amalgamation, run:

```
python amalgamate_tests.py -o tests.json -z [path/to/ethereum-test]
```


